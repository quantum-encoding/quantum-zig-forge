//! Example: Parse Binance WebSocket feed

const std = @import("std");

pub fn main() !void {
    std.debug.print("Binance Parser Example\n", .{});
    std.debug.print("TODO: Connect to wss://stream.binance.com:9443/ws/btcusdt@depth\n", .{});
}
